# 最新上游适配记录（2026-09-14）

范围：只在 `Cyrene-Agent-N/.upstream-latest` 开发；原 `Cyrene-Agent-N` 工作树和正式
`Cyrene-Agent`、正式用户数据均未写入。基线是 Gitee `playa0/cyrene-agent` 的
`eb6c311a908c4da701869fff341972bac914b4aa`。没有向上游提交 issue 或 PR。

## 本地人格稳定前缀（2026-09-19）

- `companion-chat` 升至 0.5.0，内置只读复制的本地版 `system.md`、`identity.md`、`soul.md`、
  `canon_quotes.md` 与五套 style。完整默认组合按 JavaScript 字符计约 12.5k，低于宿主单个稳定
  Provider 的 16k 上限，因此没有提高宿主限制，也没有扩大公共 API。
- 仅对原文本中明确写死的独立 Tool→Soul 阶段、旧 Playwright 插件名和“本阶段没有工具能力”措辞
  做确定性转换；上游 Harness 的工具目录、权限、参数和循环仍是唯一权威规则。人格事实、表达规则、
  原作台词与风格正文保持来源内容。
- 原生陪伴 Chat、独立聊天和插件主动消息使用同一稳定人格构建器。用户可选本地五套风格，额外文本只
  作为稳定人格末尾的追加指令；`[你的生活]`、记忆和每轮资料继续位于动态尾部，保留提示缓存边界。
- 修正 companion-chat preload 的动作白名单，使已经存在的记忆联动、生活设置、主动消息设置与手动
  主动消息测试真正可从隔离 UI 调用；未新增越权 IPC。
- 插件全量 `verify` 通过：44 个测试文件、273 项通过、11 项按条件跳过；构建产物包含人格资源，真实
  PluginManager 5 项隔离宿主测试和最终产物冒烟均通过。完整 Electron 合成原生回合进一步确认稳定
  前缀包含 Identity、Soul 与原作台词，旧双阶段/旧 Playwright 文本不存在；动态尾部同时含生活、
  历史与插件记忆。最终 `nativeTurnPassed=true`、`changedRoots=[]`，无网络或真实模型请求，临时目录已清除。

## WorldBook 插件化（2026-09-19）

- 对本地版与当前上游五份 WorldBook 做了逐文件比较：条目结构相同，但四份正文存在设定与关系表达差异；
  本地版更贴近当前陪伴目标。算法方面，本地使用 V4，上游 V5.1 增加饱和门、最近六轮重复命中抑制、
  用户命中统计与 archived 冷唤醒。经用户确认，采用“本地内容 + 上游 V5.1 算法”，未静默合并差异。
- `companion-chat` 升至 0.6.0，五份内容作为插件资源打包。解析、激活、衰减、级联和状态机均在插件
  私有实现中运行；状态只写宿主分配给插件的 `worldbook-state.json`，不读写宿主世界书状态。
- WorldBook Provider 只接受带 runId 的 `companion` 桌面 Chat。每轮先做无写入预览；仅在
  `host:turn:finished` 同时满足成功和 `finalMessageId` 已落盘时提交，失败、取消或未确认轮次丢弃。
- 陪伴后端现同时旁路宿主原生 L0/L1 与原生 WorldBook 读取/激活，避免双重注入和双重状态推进；
  默认 `native` 后端以及 Work、Code、Learn、朋友圈、渠道仍使用上游原生实现。
- 插件全量 `verify` 通过：45 个测试文件、276 项通过、11 项按条件跳过，类型检查、构建、真实
  PluginManager 隔离宿主测试与产物冒烟均通过。宿主两组定向测试 60 项通过，主进程构建成功。
- 完整隔离 Electron 合成 IPC 验收确认：本地白厄条目进入动态上下文，最终消息落盘后插件私有
  WorldBook revision 提交，记忆/生活/稳定人格链路仍通过；`nativeTurnPassed=true`、
  `changedRoots=[]`，四个正式保护根未变化，一次性目录已清除。

## 动态提示词消费回执（2026-09-19）

- Plugin API 增加可选 `consumptionReceipt` 声明和 `host:prompt:accepted` 元数据事件。Provider 仍只返回
  字符串，旧插件无需修改；旧宿主会忽略新声明但仍正常注入，因此不会因升级产生内容丢失。
- Registry 在单项 16k 与全部插件 32k 预算实际裁切后生成回执，只报告 runId、完整 Provider ID、
  接收字符数与是否完整，不包含提示词正文、记忆 ID 或凭据。完全未接收的 Provider 不产生回执。
- 回执发布会等待事件监听器被派发后再进入模型链路，避免快速固定模型先结束轮次造成竞态；监听器
  的异步工作仍保持旁路，不允许第三方插件阻塞宿主。
- `companion-memory` 0.45.0 与 `companion-chat` 0.7.0 采用双门提交：完整消费回执只确认上下文接收，
  `host:turn:finished` 的成功且 finalMessageId 存在才确认最终落盘。截断、失败、取消、未落盘或旧宿主
  无回执时均不推进 DMAE、生命周期命中或 WorldBook 激活状态。
- Registry 与宿主发布器定向测试 25 项通过，记忆/WorldBook 定向测试 17 项通过，主进程与 SDK
  构建成功。完整隔离 Electron 合成 Chat 验证回执、WorldBook 提交、记忆摄取与下一轮注入均通过；
  `nativeTurnPassed=true`、`changedRoots=[]`，四个正式保护根未变化，临时目录已清除。

## 自动主动消息与宿主最小旁路（2026-09-19）

- `companion-chat` 升至 0.3.0，新增默认关闭的自动主动消息控制器。自动模式要求桌面 Chat
  至少静默 30 分钟、两次发送至少间隔 3 小时、最多连续 2 条未回复，23:00–08:00 不发送；
  用户新消息、关闭设置、取消或插件停止都会使在途结果失效。只有受限宿主投递接口确认消息已落盘后，
  才提交冷却和未回复计数。
- 宿主增加声明式 `assistant-delivery` 能力，只允许已声明插件向宿主管理的主动消息会话写入
  1–32000 字纯助手文本；插件不能选择任意会话、伪造用户消息或改写历史。
- 桌面 Chat 选择 `companion` 且主动消息目标为 `local` 时，上游原生主动消息自动旁路，避免与插件
  重复投递。微信、飞书等渠道不属于桌面 Chat，继续使用上游原生实现；原生后端和关闭状态也保持原行为。
- 插件完整 `verify` 通过：42 个测试文件、264 项通过、11 项按条件跳过；构建、真实 PluginManager
  5 项隔离宿主测试和最终产物冒烟均通过。宿主能力、上下文、manifest 和 loader 的 5 个文件 59 项
  测试通过；主动消息后端边界 3 项测试通过；主进程重新编译成功。
- 完整隐藏 Electron 宿主验收发现并修正两处测试/构建漂移：旧 `dist` 尚未包含
  `assistant-delivery` manifest 枚举，以及隔离夹具仍检查已迁移的 `nativeChatMemoryEnabled` 字段。
  修正后两个插件均由真实主进程加载，陪伴后端门禁生效，最终 `ok=true`。
- 用户授权后，仅按绝对路径结束原版 `Cyrene-Agent/node_modules/electron/dist/electron.exe` 的进程；
  未结束其他 Electron 或应用。每次隔离运行前后均校验四个正式保护根，最终 `changedRoots=[]`，
  无真实数据、模型请求或网络访问，一次性运行目录均已清除。没有提交或推送代码。

## `[你的生活]` 插件化（2026-09-19）

- `companion-chat` 升至 0.4.0，在插件私有实现中复现本地版确定性拟态日程：本地时间凌晨 4 点
  换日，三时段各 1–2 项、同日跨时段去重，7:00–23:00 按时间窗确定当前活动，其余时间不输出
  “正在做”。原有事实边界与禁止无故催用户休息的规则保持一致。
- 纪念日不读取宿主或正式用户数据，改为插件私有配置；支持 `MM-DD 标题` 与
  `YYYY-MM-DD 标题`，默认日程开启。独立聊天、插件主动消息和陪伴后端原生 Chat 复用同一构建器。
- 动态 Prompt Provider 新增只读 `chatBackend` 元数据。日程仅在 `companion` 桌面 Chat 注入，
  原生后端、Work、Code、Learn、渠道和插件无头任务均不注入；日程不进入稳定人格前缀，因此不会
  因活动时间窗变化破坏稳定提示缓存。
- 插件全量 `verify` 更新为 43 个测试文件、269 项通过、11 项按条件跳过；宿主
  `build-options` 55 项测试通过，Plugin SDK 和主进程构建成功。
- 隐藏完整 Electron `--native-turn` 合成回合验证下一轮上下文同时含插件历史、记忆与
  `[你的生活]`，`nativeTurnPassed=true`、`ok=true`、`changedRoots=[]`；未请求网络或真实模型，
  一次性运行目录已清除。

## 插件先行

- 从原 `-N/local-plugins` 复制源码、测试、配置和文档；随后为本地验证复制现成的 `node_modules` 到新工作树。未复制旧测试运行目录或旧构建产物，也未改动正式用户数据。
- 新上游仍使用 Plugin API 1、SDK 包版本 0.2.0；新增设置面板和无头目标循环为可选契约，现有陪伴聊天与独立记忆插件不依赖它们。
- 插件类型检查、41 个测试文件的 253 项测试、三份 ZIP 构建、真实 PluginManager 的 5 项合成隔离测试与产物检查均通过。
- archived 冷召回已作为插件内人工流程完成：普通有效记忆已有明确词面/分面命中时抑制候选；否则只读展示最多 8 条有效归档候选，确认后才恢复并可严格撤销。它不接入自动注入、模型调用、DMAE 或访问统计，仍不当作“完全复刻”证明。独立聊天手动话题边界和 persona 命名空间仍属后续优化。

## 最小宿主改动

- 新增默认开启的桌面 Chat 原生记忆开关；仅显式关闭时，跳过该轮原生 L0/L1、社交原子、旧记忆提取、`chat_history` 索引及原生记忆工具。后续陪伴后端边界又把原生 WorldBook 读取与激活一并旁路；朋友圈、Work/Code/Learn、渠道和无头插件目标保持原生行为。
- 同一轮在主进程固定设置快照，防止运行中切换导致读写不一致；渲染端传来的同名字段不可信。
- 仅调整桌面 Chat 动态资料内插件上下文的相对顺序；稳定 system 前缀、消息角色和 Harness 循环不变。
- 渲染端只有真正保存终态消息后才上报落盘确认，避免插件摄取不存在的消息 ID。

## 本轮验证与边界

- 主进程 `tsc --noEmit -p tsconfig.main.json` 通过；渲染端 Vite 构建通过；相关宿主 8 个测试文件的 136 项测试通过。
- 宿主单测使用仅在测试进程有效的 `ELECTRON_OVERRIDE_DIST_PATH` 来阻止缺失 Electron 二进制触发下载；没有启动 Electron，也没有把假路径写入项目或系统配置。
- 完整 Electron 隔离入口已适配嵌套工作树，保护目录仍指向原版 `Cyrene-Agent`。原版进程运行时 `--preflight` 曾按设计拒绝启动；用户退出后，只读预检通过。
- 首次合成页面测试因未编译新工作树的 preload 而缺少 `window.chatStore`，未通过；补齐 preload 构建后重跑，原生 Chat 页面发送、终态落盘确认、插件摄取及下一轮注入通过，`ok=true`、`changedRoots=[]`。
- 用户退出原版后，又运行一次 `--real-snapshot --real-import-audit --native-turn`：只读快照含 142 条 L2、139 条证据、122 条 DMAE 状态；一次性插件导入对账含 123 条 1024 维向量，词面检索和下一轮原生 Prompt 注入均通过。源目录与快照内容校验一致，四个正式保护根前后哈希未变，7.39 GB 一次性快照及运行目录已清理。
- 入口后来收紧为预检不创建目录、合成与快照运行都只清理本次 `run-*`。本轮先前留下的三个合成／预检目录按精确路径校验后清理；再次运行合成页面回合得到 `ok=true`、`changedRoots=[]`，并确认它的运行目录在退出后不存在。
- 以上证明新版宿主的隔离装配、合成回合和真实数据副本迁移链路，不是实时真实模型回答质量验收，也不证明本地版 Chat/Collab 已完全复刻。

## 受控真实主模型回复（2026-09-15）

- 上游 Gitee `master` 仍为上述 `eb6c311a` 基线。原版模型配置只读核对为 Kimi `kimi-k2.6`；没有输出或复制 API 密钥。
- 先运行完整宿主隔离入口的 `--preflight`，只读确认原版进程已退出、四个正式数据根可校验；该步没有启动 Electron 或创建测试目录。
- 在 `-N` 中以固定合成会话／合成薄荷记忆调用原生 `buildAgentRunOptions`、Harness Prompt 分层和 Kimi 适配器。插件记忆只在动态上下文，稳定前缀未包含它；关闭原生记忆和工具，单次非流式请求、`max_tokens=256`。请求体 19,330 字符，提示词及凭据没有写入报告。
- 唯一一次真实 Kimi 请求返回 HTTP 200、`finish_reason=stop`，回复为“你刚才说，今天想给窗台上的薄荷浇水呀。”；模型用量为输入 8,836、输出 166 token。回复命中合成事实，未出现工具协议文本。由于输入 token 高于字符数预估，没有追加调用。
- 请求前后对原版 Roaming/Local Cyrene、项目 `UserData` 与 `UserData_backup` 四个根做完整只读哈希；`changedRoots=[]`。仅在 `-N` 写入不含提示词正文／密钥的 [结果 JSON](./BOUNDED-KIMI-CHAT-CHECK.json)。没有启动原版或新版完整主程序，也没有写入任何正式数据。
- 这验证了原生 Chat 组装链路可以用主模型消费插件动态记忆并生成自然回复；不覆盖真实历史检索质量、插件私有记忆摄取、带工具 Harness 多轮、完整 Electron 真模型回合或与本地版的逐项回复质量对比。

## 后续隔离验证（2026-09-15）

- 再次只读预检通过，原版 Cyrene 已退出；Gitee 上游 `master` 仍是 `eb6c311a`。
- 增加一个不联网的带工具桌面 Chat 投影测试：启用 Chat 工具但关闭原生记忆时，Kimi 请求体保留非记忆工具 `weather`，不包含 `user_memory`；插件记忆只进入动态消息，不进入稳定 system 前缀，`prompt_cache_key` 保持稳定层格式。主程序三组定向测试共 77 项通过，主进程 TypeScript 类型检查通过。
- 完整 Electron 隐藏式 `--native-ui-turn` 再次通过：原生页面发送、终态消息落盘确认、插件摄取、下一轮原生注入均成功。隔离宿主阻断模型、外网和截图辅助进程；四个正式保护根前后哈希一致，`ok=true`、`nativeTurnPassed=true`、`changedRoots=[]`，本次 `run-*` 目录在退出时清除。
- 本次没有再次调用 Kimi。离线工具投影和完整页面隔离测试仍不能替代“同一完整 Electron 回合内真实调用模型”的验收。

## 完整 Electron 原生 Chat 单次主模型验收（2026-09-15）

- 在原隔离入口新增独立 `--bounded-model-ui-turn` 模式，先仅以固定 SDK 流式回包做零 token 彩排。首次彩排发现原生 Chat 的朋友圈内置工具默认可见，意外走入 Harness，模型网络仍被阻断、`liveCalls=0`、正式数据 `changedRoots=[]`、临时目录已清除。随后只在测试合成设置中显式关闭这三个内置工具，第二次彩排走未替换的原生 `ChatLoop` 并通过：首轮页面回复落盘与插件摄取、第二轮插件资料进入模型请求、页面落盘与再次摄取，固定模型回包 2 次、外发 0 次。
- 彩排通过后，严格单独运行 `--bounded-model-ui-turn --live-model`。首轮仍为不外发的固定回包，第二轮才通过只允许 `api.moonshot.cn/v1/chat/completions` 的主进程请求闸门和单次 TLS 连接，调用原版只读配置的 Kimi `kimi-k2.6`。测试层把该次流式请求的 `max_tokens` 限为 512；不允许工具、真实历史、非合成问题或额外后台模型请求。原生 Agent、ChatLoop、SDK、渲染页与插件均未替换。
- 真实回合 `ok=true`；固定回包 1 次、真实 Kimi 请求 1 次、意外模型请求 0 次。第二轮请求含首轮已落盘的内容与插件参考资料，回复命中合成薄荷／浇水事实且在原生页面保存，插件确认再次摄取。模型上报本次真实调用输入 7,590、输出 158 token；宿主模型请求统计为 2 次，其中首轮固定回包用量为 0。正式 Roaming/Local Cyrene、项目 `UserData` 和备份四根前后完整哈希一致，`changedRoots=[]`；本次一次性目录（包括隔离模型配置副本）在退出时清除，日志未发现密钥泄漏。
- 可复用的[结果摘要](./BOUNDED-ELECTRON-KIMI-CHECK.json)仅存储调用计数、用量、布尔验收与隔离结论，不存储密钥、真实用户消息或模型请求体。这项“完整 Electron 无工具原生 Chat 的真实模型回合”已通过，不应因为测试准备问题重复调用。仍未覆盖真实历史／记忆快照的完整模型回复、带工具 Harness、多样本本地效果对照以及失败／取消路径。

## 真实快照原生 ChatLoop 零 token 验收（2026-09-15）

- 扩展隔离入口，使 `--real-snapshot --real-import-audit --bounded-model-ui-turn` 只允许在隐藏、**不带 `--live-model`** 的模式运行；原有真实模型合成模式不变。先以提升的只读权限完成四个正式数据根的 `--preflight`，未启动 Electron 或创建目录。
- 一次性复制正式 Roaming 数据约 7.34 GB 到 `-N`，内容哈希与源一致。插件空库导入对账通过：148 条 L2、144 条证据、127 条 DMAE 状态、124 条 1024 维向量；只读词面检索选中一条长度受限的真实记忆。测试未修改原记忆访问次数、DMAE 或激活值。
- 未替换原生 `CyreneAgent`／`ChatLoop`／SDK／渲染页。首轮合成消息由固定 SDK 流回包，落盘后由插件摄取；第二轮以真实记忆触发片段查询，隔离网络闸门确认原生模型请求同时包含插件参考资料和该记忆正文，再返回固定回复。两次都是本地固定回包，真实请求 0 次、模型输入和输出 token 均为 0；第二轮回复仍保存到原生 Chat，插件再次摄取。真实记忆正文及请求体没有写入报告或日志。
- `ok=true`、`nativePromptInjectionPassed=true`、`realSnapshotMemoryInjected=true`、`changedRoots=[]`，快照源与副本内容校验一致；本次 7.34 GB 快照与运行目录在程序退出后一同清除。随后 `local-plugins` 完整 `npm.cmd run verify` 通过：41 个文件／253 项测试、3 个 ZIP、5 项宿主测试及产物冒烟；`node --check` 和 `git diff --check` 通过。
- 这项证明真实记忆副本可以进入未替换的原生 ChatLoop 的模型请求，**不证明模型对真实历史的回复质量**；带工具 Harness、取消／失败路径和本地版效果对照仍待验收。为避免无意义的模型 token 消耗，本次没有重复调用 Kimi。

## 真实记忆主模型验证待授权（2026-09-15）

- 为一次性 Kimi 验收增加只允许官方 `api.moonshot.cn/v1/chat/completions` 的单 POST、最多 512 输出 token、插件记忆块最多 6000 字符及至多 8 条真实记忆的闸门；不改插件的原生检索排序或数量。请求与凭据不写日志。
- 先完成四个正式数据根的只读预检，再以完整真实快照零外发彩排。彩排显示正常原生检索注入 8 条记忆、参考资料块 3794 字符，并命中预先选定的记忆；先前设定的“至多 3 条”测试闸门因此拒绝第二轮固定回包，**真实模型调用为 0**。正式数据 `changedRoots=[]`，快照内容与源一致，运行目录已清理。已将闸门修正为原生 8 条上限；基于同一次彩排元数据，没有为计数变化再次复制快照。
- 随后的 `--live-model` 命令在进程创建前被权限审查拒绝，理由是尚未针对把这批真实记忆及证据发送至外部 Kimi 服务取得具体授权。没有启动 live 测试、创建新的快照或发送任何真实数据；不得换入口绕过拒绝。待用户明确授权外发范围后才可继续一次性测试。

## 获具体授权后的一次真实请求（2026-09-15）

- 用户对“最多 8 条真实记忆及对应来源证据、资料块不超过 6000 字符、单次发送至 Kimi 官方服务”的具体问题回复 `ok`。重新只读预检通过，原版 Cyrene 进程未运行；在 `-N` 内建立约 7.34 GB 一次性快照，导入对账与词面命中通过。
- 隔离入口记录固定回包 1 次、真实 Kimi 请求 **1 次**、意外模型请求 0 次。第二轮模型请求包含被命中的真实记忆，原生前 8 条参考资料块 3794 字符，没有超过授权范围；Kimi 的 HTTP 响应被接受，但流式路径最终没有返回可见文本，原生 Chat 报 `E_MODEL_RESPONSE_PARSE_FAILED`，未保存第二轮回复，故 `ok=false`。无凭据、真实记忆正文或请求体写入报告。
- 这次真实请求把流式输出强制限制到 `max_tokens=512`；正式配置是 `kimi-k2.6` 且推理模式为 `auto`。相较上游正常流式请求不强制该上限，推理耗尽 512 token 是**待证实的可能原因**；这次没有保留流结束原因或用量，不能声称已经定位根因，也不能声称真实回复验收通过。
- 四个正式保护根前后完整哈希一致（`changedRoots=[]`），源与快照内容一致；临时快照及隔离密钥副本在退出后一同清除。原授权限定为一次外发，已用完；不再追加模型请求。下一次如需验证，应先离线完善流结束原因／用量的隐私安全诊断与输出预算，再取得新的具体授权。

## 加大预算后的真实记忆回复验收（2026-09-15）

- 用户明确要求“把预算改大再试一次”。仅为真实快照 live 入口把流式 `max_tokens` 从 512 提至 4096，并延长隔离页面等待／看门狗；合成和其他入口保持原行为。增加只记录 SSE 结束原因、可见／推理字符数及 token 用量的测试诊断，不记录回复、推理、记忆正文或请求体。`node --check` 通过，零外发合成完整页面彩排再次通过，正式数据 `changedRoots=[]`。
- 在原版 Cyrene 退出且四个正式数据根只读预检通过后，重新建立完整一次性快照。隔离导入对账为 148 条 L2、144 条证据、127 条 DMAE、124 条 1024 维向量；原生前 8 条参考资料块 3794 字符，含被检索命中的真实记忆。
- 首轮仅固定流回包；第二轮向官方 Kimi `kimi-k2.6` 发送真实请求 **1 次**，意外模型请求 0 次。流结束原因为 `stop`，可见文本 72 字符、推理文本 2162 字符（只记录长度）；模型上报输入 9487、输出 1323 token。原生 Chat 页面保存了非空、无工具协议的回复，插件确认摄取第二轮；`ok=true`、`nativeTurnPassed=true`、`realSnapshotMemoryInjected=true`、`nativePromptInjectionPassed=true`。
- 正式 Roaming/Local Cyrene、项目 `UserData` 与备份四根前后哈希一致（`changedRoots=[]`），快照源与副本内容一致；一次性快照和隔离密钥副本退出后已清除。随后插件完整 `npm.cmd run verify` 通过：41 个文件／253 项测试、3 个 ZIP、5 项宿主测试与产物冒烟。
- 先前 512 上限下的空可见文本与本次 4096 上限下成功回复形成较强关联，但先前未记录结束原因，不能严格证明唯一原因就是预算。此项已证明完整 Electron 原生 ChatLoop 能消费真实记忆副本并生成、落盘、再次摄取真实模型回复；**不证明**多样本回复质量与本地版一致，带工具 Harness、取消／失败路径及本地版效果对照仍待验收。
