# 插件模板

用于验证本地 Cyrene 插件开发环境、官方 SDK 契约、构建和生命周期清理。它只注册一个
只读健康检查工具，不提供实际业务功能。

## 启用和使用

导入插件后需要用户手动启用。仅在需要检查插件模板是否成功加载时，让 Cyrene 调用
“检查插件模板状态”工具。

## 宿主能力

不申请任何 `deps`。

## 数据、网络与系统行为

- 不访问网络；
- 不读取或写入文件；
- 不启动子进程；
- 不读取 Cyrene 会话或用户数据；
- 不保存插件私有数据或密钥。

## 已知限制

这是开发模板，不应作为面向普通用户的功能插件发布。
